# Deep-Neural-Network-Forward-and-Backward-Pass
A complete implementation of a Deep Neural Network from scratch using NumPy, including forward propagation, backpropagation, binary and multiclass classification, ReLU, Sigmoid, Softmax, Negative Log-Likelihood loss, and gradient computation without relying on deep learning frameworks.
